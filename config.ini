# OpenClash Rule Generator INI (локальные, CDN, антифильтр, резерв)
enable_rule_generator=true
overwrite_original_rules=true

# --- Локальные сети напрямую ---
ruleset=DIRECT,https://raw.githubusercontent.com/Loyalsoldier/clash-rules/release/lancidr.txt,ipcidr
ruleset=DIRECT,https://raw.githubusercontent.com/Loyalsoldier/clash-rules/release/private.txt,ipcidr

# --- CDN (можно выбрать DIRECT или через прокси) ---
ruleset=📦 CDN,https://raw.githubusercontent.com/Loyalsoldier/clash-rules/release/cdn.txt,domain

# --- antifilter (ipcidr-список) — через прокси ---
ruleset=🚀 Выбор_сервера,https://antifilter.download/list/ip.lst,ipcidr

# --- финальное правило — всё остальное через выбранный прокси ---
ruleset=🚀 Выбор_сервера,[]FINAL

# --- Proxy groups ---
# Основная группа: ручной выбор / автотест / напрямую
custom_proxy_group=🚀 Выбор_сервера`select`[]♻️ Автовыбор`[]DIRECT`.*
# Автовыбор (пинг-тест)
custom_proxy_group=♻️ Автовыбор`url-test`.*`http://www.gstatic.com/generate_204`300,,50
# Резервная группа на случай недоступного узла
custom_proxy_group=🛟 Резерв`fallback`.*`http://www.gstatic.com/generate_204`300
# CDN-группа: можно DIRECT или через основной прокси
custom_proxy_group=📦 CDN`select`[]DIRECT`[]🚀 Выбор_сервера
